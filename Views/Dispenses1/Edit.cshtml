@model HospitalMgt.Models.Dispense

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>
</head>
<body>

<h4>Dispense</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="DispensedBy" class="control-label"></label>
                <select asp-for="DispensedBy" class="form-control" asp-items="ViewBag.DispensedBy"></select>
                <span asp-validation-for="DispensedBy" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DispensedTo" class="control-label"></label>
                <select asp-for="DispensedTo" class="form-control" asp-items="ViewBag.DispensedTo"></select>
                <span asp-validation-for="DispensedTo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
</body>
</html>
